\# Hands-on 7 - Add Country



Implemented adding a new country using Spring Data JPA.



\### Features

\- Added `addCountry()` in `CountryService`

\- Used `JpaRepository.save()`

\- Inserted a new country into MySQL

\- Verified the inserted record using `findCountryByCode()`

