\# Exercise 2 - Implementing Dependency Injection



This exercise demonstrates Dependency Injection using Spring IoC with XML configuration.



\- Created `BookService` and `BookRepository` classes.

\- Configured beans in `applicationContext.xml`.

\- Injected `BookRepository` into `BookService` using setter injection.

\- Loaded the Spring context and verified the dependency injection by running the application.

